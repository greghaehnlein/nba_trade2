<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @team.name %>
</p>
<p><%= image_tag @team.image, size: 20 %></p>
	<strong>Players on this Team:</strong>

	<% @team.players.each do |player| %>
	<ul>
		<li><%= player.name %></li>
	</ul>	
		
	<% end %>

<%= link_to 'Edit', edit_team_path(@team) %> |
<%= link_to 'Back', teams_path %>
